/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['flowbite.com', 'tailwindui.com', 'images.unsplash.com'],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
